version: '3.8'

services:
  postgres:
    image: timescale/timescaledb:latest-pg15
    container_name: finanalytics-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: finanalytics
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always

  redis:
    image: redis:7-alpine
    container_name: finanalytics-redis
    ports:
      - "6379:6379"
    restart: always

volumes:
  postgres_data:
